models:
  resnet50:
    num_classes: 10
    pretrained: True
    input_size: 224
    classifier:
      dropout: 0.3
      hidden_dim: null
    fine_tuning:
      freeze_backbone: False
      unfreeze_from_layer: 'layer1'
    use_batch_norm: True

  efficientnet_b3:
    num_classes: 10
    pretrained: True
    input_size: 300
    classifier:
      dropout: 0.3
      hidden_dim: null
    fine_tuning:
      freeze_backbone: False
      unfreeze_from_layer: 'layer1'
    use_batch_norm: True